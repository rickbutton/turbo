
allow specifying target location in layout
- will likely need to runtime restrict to 1 target

tests for cli, logger, connector-node, net/server

layout strings (at least a good default)

cdp integration into shared state
components render, send actions to daemon?
- do i need to care about reconciling conflicts?
  - global counter, inc each action
  - or counter per topic/type of state?

better logger
better "press enter to restart message"

ls command
