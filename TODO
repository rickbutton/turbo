allow specifying target location in layout
- will likely need to runtime restrict to 1 target

tests for cli, logger, connector-node, net, core/reducer, core/v8

layout strings (at least a good default)

better logger
better "press enter to restart message"

ls command

automatically kill tmux when entire session dies?
- how to determine when session dies?

listen for log events and
broadcast as dangling messages to clients, then
implement log component that streams in log messages
with optional syntax highlighting

object expand - allow expanding automatially, defer fetching
nested props until interaction

context + hooks for mouse focus and hover (for hovering values in code)

tooltip widget - React portal?
