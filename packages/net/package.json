{
  "name": "@turbo/net",
  "version": "1.0.0",
  "main": "lib/index",
  "types": "lib/index",
  "files": [
    "lib"
  ],
  "scripts": {
    "build": "npm run clean && npm run compile",
    "watch": "npm run build && npm run compile-watch",
    "clean": "rm -rf ./lib",
    "compile": "tsc -p tsconfig.build.json",
    "compile-watch": "tsc -w -p tsconfig.build.json",
    "prepublishOnly": "npm run build"
  },
  "devDependencies": {
    "@types/node": "^12.12.5",
    "typescript": "^3.6.4"
  },
  "dependencies": {
    "@turbo/core": "^1.0.0"
  }
}
